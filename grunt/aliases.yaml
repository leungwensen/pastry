
build:
  - jshint
  - compileTemplates
  - requirejs
  - jasmine
  - promiseAPlusTests
  - less
  - copy:build
  - copy:locale

dist:
  - build
  - clean
  - copy:dist
  - copy:locale
  - uglify
  - cssmin

travis:
  - jshint
  - compileTemplates
  - requirejs
  - jasmine
  - promiseAPlusTests

test:
  - travis

demo:
  - connect:demo
  - watch

default:
  - build

dev:
  - build

release:
  - dist

