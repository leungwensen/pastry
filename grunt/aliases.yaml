
build:
  - jshint
  - compileTemplates
  - requirejs
  - jasmine
  - less
  - copy:build

dist:
  - build
  - clean
  - copy:dist
  - uglify
  - cssmin

travis:
  - jshint
  - compileTemplates
  - requirejs
  - jasmine

demo:
  - connect:demo
  - watch

default:
  - build

dev:
  - build

release:
  - dist

