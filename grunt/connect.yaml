options:
  debug: true
  keepalive: false
  open: http://<%= host %>:<%= port %>
  port: <%= port %>
  hostname: <%= host %>
  livereload: <%= livereload %>
  base: ''

demo-ui:
  options:
    open: http://<%= host %>:<%= port %>/demo/ui